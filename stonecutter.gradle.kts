plugins {
    id("dev.kikugie.stonecutter")
}
stonecutter active "2026.1"

repositories {
    maven("https://www.jetbrains.com/intellij-repository/releases") {
        name = "IntelliJ Repository (Releases)"

    }
}